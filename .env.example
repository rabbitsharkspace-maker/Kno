# Optional. Kno is free and every reader brings their own key, so this is only
# for running a build that ships one — and `vite.config.ts` inlines it into the
# public bundle, so never set it on a deploy anyone else can load.
GEMINI_API_KEY=
